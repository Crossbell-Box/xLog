apiVersion: v1
kind: Service
metadata:
  name: xlog
  namespace: crossbell
spec:
  type: ClusterIP
  selector:
    app: xlog
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
